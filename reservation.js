"use strict";

$(document).ready( () => {
	const emailPattern = /\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,4}\b/;
	  
	  //  Moved focus to the Pickup Date text box
 	  $("#pickup_date").focus();	
}); // end ready
